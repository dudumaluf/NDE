# Copie para .env e preencha. NUNCA commitar o .env.

# Chave do fal.ai (dashboard → Keys) — usada por transcribe (wizper) e extract (any-llm)
FAL_KEY=

# Alternativa para extração direta na Anthropic (plano B, backend=anthropic)
ANTHROPIC_API_KEY=
